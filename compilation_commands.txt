// For compiling basic single process matrix multiplication.
gcc runnables/sp_mmult.c sources/sqmatrix.c sources/benchmark.c -o sp_mmult

// For compiling IPC pipe program using semaphore and single pipe.
gcc runnables/ipc-pipe.c sources/sqmatrix.c sources/benchmark.c sources/locks.c -o ipc_pipe

// For compiling IPC pipe program using semaphore and single pipe.
gcc runnables/ipc-shmem.c sources/sqmatrix.c sources/benchmark.c sources/locks.c -o ipc_shmem

// For perf recording
 sudo perf record -g -o <file name> <program name>

// For perf reading
sudo perf report -i <file name> 